import Navbar from '../../molecules/Navbar/Navbar';
import './_header.scss';

function Header(props) {
    return (
        <header>
            <Navbar />
        </header>
    );
}

export default Header;